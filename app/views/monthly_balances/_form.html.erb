<%= form_for(@monthly_balance) do |f| %>
  <% if @monthly_balance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@monthly_balance.errors.count, "error") %> prohibited this monthly_balance from being saved:</h2>

      <ul>
      <% @monthly_balance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :account_id %><br>
    <%= f.number_field :account_id %>
  </div>
  <div class="field">
    <%= f.label :month %><br>
    <%= f.number_field :month %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :balance %><br>
    <%= f.number_field :balance %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
